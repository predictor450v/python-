# install an external module and use it 

# open terminal >> write pip name >>copy use code line >> now use it 

import pyttsx3
engine = pyttsx3.init()
engine.say("ayush is best")
engine.runAndWait()
